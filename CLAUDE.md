# CLAUDE.md

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.

## 핵심 원칙

1. **프레임워크 없음** — React/Vue/Svelte/Next 등 UI 프레임워크, Tailwind 같은 CSS 프레임워크,
   jQuery 등 런타임 라이브러리를 쓰지 않는다. 브라우저로 나가는 코드는 순수 HTML/CSS/JS만.
2. **읽기 경험 우선** — 본문 가독성이 모든 디자인 판단의 기준. 장식이 읽기를 방해하면 뺀다.
3. **점진적 향상** — JS가 실패해도 글은 읽혀야 한다. JS는 다크 모드 토글, 목차 하이라이트 등
   부가 기능만 담당한다.

## 구조

```
site.config.js      # 블로그 제목·저자·배포 주소. 사이트 정보는 여기만 고친다
posts/              # 원본 마크다운 (.md) — 글은 여기에만 추가한다
src/
  build.js          # 엔트리: posts/ → dist/
  serve.js          # 로컬 정적 서버 (node:http 만 사용, 의존성 0)
  lib/
    markdown.js     # marked 설정, 하이라이팅, 헤딩 id, 표 래핑
    template.js     # 템플릿 치환 + 이스케이프 + slugify
  templates/
    base.html       # 문서 뼈대 (head, 헤더/푸터, 테마 인라인 스크립트)
    index.html      # 글 목록
    post.html       # 글 페이지
    tag.html        # 태그별 목록
    tags.html       # 전체 태그
  assets/
    style.css       # 전체 스타일 (단일 파일)
    theme.js        # 다크 모드 토글
dist/               # 빌드 산출물. 직접 수정 금지 — 항상 소스를 고친다
```

## 명령

```bash
npm run dev
```

`dev` 는 빌드 후 `posts/` 와 `src/` 를 감시하며 저장할 때마다 다시 빌드한다 (http://localhost:3000).
빌드만 하려면 `npm run build`, 이미 만든 결과만 서빙하려면 `npm run serve`.

## 의존성 방침

브라우저로는 **어떤 의존성도 전송하지 않는다.** 각 페이지가 받는 파일은 HTML·CSS·JS 3개뿐이고
외부 도메인 요청은 0건이다. 아래 3개는 빌드 타임에만 쓴다.

| 패키지 | 용도 |
|---|---|
| `marked` | 마크다운 → HTML |
| `highlight.js` | 코드 하이라이팅 (빌드 시점에 실행) |
| `gray-matter` | YAML 프론트매터 파싱 |

새 런타임 스크립트나 웹폰트 CDN을 추가하기 전에 반드시 상의한다.

## 마크다운 규약

각 글은 YAML 프론트매터로 시작한다:

```markdown
---
title: 글 제목
date: 2026-01-15
tags: [css, 회고]
description: 목록과 og:description에 쓰이는 한 줄 요약
draft: false
---
```

- `title`, `date` 는 필수. 나머지는 선택.
- `draft: true` 인 글은 빌드에서 제외한다.
  저장소가 공개이므로 **초안은 `.gitignore` 에도 파일명을 추가한다** — 빌드에서 빠져도
  원문이 저장소에 올라가면 그대로 읽힌다.
- 출력 경로는 파일명 기준: `posts/2026-01-15-title.md` → `dist/posts/2026-01-15-title.html`

## 빌드 규칙

- 마크다운은 빌드 시점에 HTML로 변환한다. **클라이언트에서 파싱하지 않는다.**
  코드 하이라이팅도 마찬가지 — 런타임 하이라이터 스크립트를 넣지 않는다.
- **필수 항목이 빠지면 빌드를 세운다.** `title`/`date` 누락, 잘못된 날짜 형식은
  파일명을 포함한 메시지와 함께 실패한다. 조용히 넘어가지 않는다.
- 템플릿 치환은 `{{key}}` 가 **기본 이스케이프**다. 이미 HTML인 값만 `{{{key}}}` 로 통과시킨다.
  대응하는 값이 없는 키는 빈 문자열이 아니라 에러다 (템플릿 오타를 빌드에서 잡기 위함).

### 경로는 전부 상대 경로

배포 위치가 정해지지 않았으므로 절대 경로를 쓰지 않는다. 페이지 깊이에 따라 접두사를 계산해
주입한다 — 루트는 `''`, 한 단계 아래는 `'../'`. 이래야 GitHub Pages 서브경로든 루트 도메인이든
손대지 않고 올라간다. `site.config.js` 의 `url` 은 canonical/og:url 에만 쓰이고,
비어 있으면 해당 태그를 생략한다.

### 생성되는 페이지

| 경로 | 내용 |
|---|---|
| `index.html` | 글 목록 (최신순) |
| `posts/<파일명>.html` | 글 페이지 + 이전/다음 글 |
| `tags/<태그>.html` | 태그별 글 목록 |
| `tags/index.html` | 전체 태그 |

## 디자인

### 색상 — CSS 변수 하나로 통일

모든 색은 `:root` 에 정의한 토큰만 쓴다. 하드코딩된 `#fff`, `rgb(...)` 를 규칙 안에 직접 쓰지 않는다.

```css
:root {
  --bg: #fdfdfc;
  --bg-subtle: #f4f4f2;
  --text: #1a1a18;
  --text-muted: #6b6b66;
  --border: #e4e4e0;
  --accent: #2f6f4e;
  --code-bg: #f4f4f2;
}
```

다크 모드는 같은 토큰 이름을 재정의하는 방식으로만 구현한다. 다크 전용 셀렉터를 새로 만들지 않는다.

```css
:root[data-theme="dark"] { --bg: #161614; /* ... */ }
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) { --bg: #161614; /* ... */ }
}
```

문법 하이라이팅 색(`--syn-keyword`, `--syn-string`, `--syn-comment` …)도 같은 토큰 체계에 넣는다.
highlight.js 기본 테마를 가져오면 라이트/다크가 별도 파일이라 이 체계가 깨진다.

- 다크 모드에서 순수 검정(`#000`)과 순수 흰색 본문 텍스트는 쓰지 않는다. 눈이 아프다.
- 대비는 본문 4.5:1, 큰 텍스트 3:1 이상을 지킨다. **문법 색도 예외가 아니다** —
  라이트용으로 고른 색을 어두운 배경에 그대로 올리면 대비가 가장 먼저 무너지는 곳이 주석이다.

### 다크 모드 동작

- 상태는 3가지: `light` / `dark` / 시스템 설정 따름(기본, `data-theme` 속성 없음).
- 선택값은 `localStorage` 에 저장한다.
- **FOUC 방지**: `<head>` 안 인라인 `<script>` 에서 CSS 로드 전에 `data-theme` 를 지정한다.
  이 스크립트만은 외부 파일로 분리하지 않는다.
- 토글 버튼은 `<button>` 이어야 하고 `aria-label` 을 붙인다.

### 타이포그래피

- 본문 폭은 `65ch` 내외. 화면이 넓어도 한 줄이 길어지지 않게 한다.
- 본문 `line-height: 1.7` 이상, 기본 크기 `18px` 내외.
- 한글 가독성을 위해 시스템 폰트 스택을 우선한다. 웹폰트를 추가할 때는 `font-display: swap`
  과 한글 서브셋을 함께 쓴다.
- 제목 계층은 크기 차이만으로 구분한다. 색이나 밑줄로 요란하게 구분하지 않는다.

### 반응형

- 모바일 우선으로 작성하고 `min-width` 미디어 쿼리로 넓혀간다.
- 고정 픽셀 폭 금지. `max-width`, `rem`, `ch`, `clamp()` 를 쓴다.
- 표, 코드 블록, 넓은 이미지는 각자 `overflow-x: auto` 컨테이너 안에서 스크롤한다.
  **페이지 본문(`body`)은 절대 가로 스크롤이 생기면 안 된다.**
- 이미지는 `max-width: 100%; height: auto;` 를 기본으로 한다.
- 탭 가능한 요소의 터치 타깃은 최소 44×44px.

### 접근성

- 시맨틱 태그를 쓴다: `<article>`, `<header>`, `<nav>`, `<main>`, `<time datetime="...">`.
- 포커스 링을 제거하지 않는다. 필요하면 `:focus-visible` 로 다시 디자인한다.
- 본문 건너뛰기 링크(skip link)를 넣는다.

## 배포

`main` 에 push 하면 `.github/workflows/deploy.yml` 이 빌드해서 GitHub Pages 로 올린다.
`dist/` 는 커밋하지 않는다 — Actions 가 매번 새로 빌드한다.

- 주소: https://cjh3131473-hash.github.io/my-blog/ (서브경로 배포)
- 모든 링크가 상대 경로라 서브경로에서도 그대로 동작한다. 이 규칙을 깨지 않는다.
- `site.config.js` 의 `url` 은 canonical/og:url 에만 쓰인다. 주소가 바뀌면 여기도 고친다.

## 코드 스타일

- 빌드 스크립트는 ES 모듈(`"type": "module"`), Node 내장 모듈 위주.
- 브라우저로 나가는 `theme.js` 는 빌드 단계가 없으므로 옛 문법으로 보수적으로 쓴다.
- CSS는 단일 파일에서 다음 순서로:
  토큰 → 리셋 → 레이아웃 → 타이포 → 컴포넌트 → 코드 하이라이팅 → 미디어 쿼리.
- 클래스명은 `post-meta`, `toc-link` 처럼 소문자-하이픈.
- 주석은 "왜"만 남긴다. 코드가 설명하는 것을 반복하지 않는다.

## 바꾼 뒤 확인할 것

디자인이나 템플릿을 건드렸으면 최소한 이만큼은 직접 본다.

1. 다크로 두고 하드 리로드 — 흰 화면이 번쩍이지 않는가 (FOUC)
2. 라이트를 고른 뒤 OS를 다크로 변경 — 사이트는 라이트를 유지하는가
3. 뷰포트 375px — 모든 페이지에서 `body` 에 가로 스크롤이 없는가.
   표와 코드 블록은 각자 컨테이너 안에서만 스크롤되는가
4. 코드 색이 **라이트/다크 양쪽에서** 4.5:1을 넘는가
5. JS를 끈 상태에서 글이 읽히는가 (테마 버튼은 JS가 있을 때만 드러난다)
6. 네트워크 탭에 외부 도메인 요청이 0건인가

## 하지 말 것

- `dist/` 를 직접 수정하기 (다음 빌드에 날아간다)
- 다크 모드용 CSS 규칙을 별도로 복제해 만들기 (토큰 재정의로 해결한다)
- 클라이언트 사이드 마크다운 파싱
- 애니메이션 남용 — `prefers-reduced-motion: reduce` 를 항상 존중한다
- 분석 스크립트, 폰트 CDN 등 외부 요청을 상의 없이 추가하기
